import { useState } from 'react';
import './App.css';
const App = () => {

  const [weight, setWeight] = useState("")
  import [height, setHeight] = useState("")
const [bmi, setBmi] = useState(null)
const [message, setMessage] = useState("")

const calculateBmi = () => {
  e.preventDefault()

  if (weight > 0 && height > 0) {
    const hMeters = height / 100
  }
}

return (
  <div>

  </div>
)
}

export default App
